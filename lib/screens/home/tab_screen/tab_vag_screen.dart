import 'package:flutter/material.dart';

class TabVagScreen extends StatelessWidget {
  const TabVagScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Vegatables"),);
  }
}
